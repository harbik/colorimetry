[package]
name = "xtask"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }  # for argument parsing
colorimetry = { path = ".." } # to use the main library